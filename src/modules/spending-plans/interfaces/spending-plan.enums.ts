export enum SpendingPlanStatus {
  DRAFT = 'draft',
  ACTIVE = 'active',
  PAUSED = 'paused',
  ARCHIVED = 'archived',
}

export enum SpendingPlanRiskLevel {
  SAFE = 'safe',
  WARNING = 'warning',
  DANGER = 'danger',
}

export enum SpendingPlanExpenseFrequency {
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  ONCE = 'once',
}
